# chromium-latest-linux
Scripts to download and run the latest Linux build of Chromium.

## Instructions

### Running the scripts

To run the scripts, use the following commands:

```bash
./run.sh
```

### Updating and running the scripts

To update and run the scripts, use the following commands:

```bash
./update-and-run.sh
```
